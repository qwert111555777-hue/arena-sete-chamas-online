{
  "name": "arena-sete-chamas-online",
  "version": "0.1.0",
  "description": "Arena 2D online no navegador para ate 5 jogadores, com salas, herois personalizados e chefes.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "verify": "node --check server.js && node --check public/client.js && node tools/verify_game.js && python3 tools/verify_visual.py"
  },
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
